# 功能
"中间代理"：解耦"前端业务"和"后端业务"，提供统一的"请求参数"与"响应格式"，前端将请求发送给“中间代理”（参数为“请求参数”），“中间代理”在调用“后端”提供的PAI接口，并将执行请求后的结果写入到“响应格式”，然后发送给中间代理，中间代理再对返回的结果进行检查（成功/失败等），然后返回给前端，前端获得数据后再进行渲染

# 优点
解耦了“前端”与“后端”，“前端”与“后端”可以独立并行开发，只需要符合“代理”规定的“请求参数”与“响应格式”即可。